package Result;

/** Holds the data for what's returned (either a fail statement, an event, or an array of events) */
public class EventResult {

    String result;

    /** Builds the EventResult with the string given to it. Could be success or fail.
     *
     * @param result*/
    public EventResult(String result) {

    }
}
