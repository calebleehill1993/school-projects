package Result;

/** Holds the data for what's returned (either a fail statement, a person, or an array of persons) */
public class PersonResult {

    String result;

    /** Builds the PersonResult with the string given to it. Could be success or fail.
     *
     * @param result*/
    public PersonResult(String result) {

    }
}
